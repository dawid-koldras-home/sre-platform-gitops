apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: synthetic-monitoring
  namespace: argocd
  annotations:
    argocd.argoproj.io/refresh: hard
  labels:
    app.kubernetes.io/part-of: sre-platform
    app.kubernetes.io/managed-by: srectl
    sre.platform.io/tenant: "sre-tools"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: "https://github.com/dawid-koldras-home/sre-platform-gitops.git"
    targetRevision: "main"
    path: "platform/modules/synthetic-monitoring"
  destination:
    server: https://kubernetes.default.svc
    namespace: "synthetic-monitoring"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
